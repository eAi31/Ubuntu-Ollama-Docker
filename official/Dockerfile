# Use the official Ollama image
FROM ollama/ollama:latest

# Expose the default port
EXPOSE 11434

# Optional: set environment variables or volumes as needed
# ENV OLLAMA_HOST=0.0.0.0:11434
# VOLUME ["/root/.ollama"]

CMD ["serve"]
